// Temporary demo mode — remove when backend is ready.
export const DEMO_CHECKOUT = process.env.NEXT_PUBLIC_DEMO_CHECKOUT === "true";

// Temporary admin mocks — switch to backend API when ready
export const USE_ADMIN_MOCKS = true; // Force to true for debugging
